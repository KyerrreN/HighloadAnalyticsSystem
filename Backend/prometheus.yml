global:
  scrape_interval: 5s # to be fine tuned, cuz its good for dev, but for prod it's better to scrape less to conserve storage

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]
